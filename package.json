{
  "name": "cross-domain-request",
  "version": "1.0.0",
  "description": "Make POST/GET cross-domain requests without CORS",
  "keywords": ["CORS", "cross domain request", "post request"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/fliptheweb/cross-domain-request.git"
  },
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "npm run build",
    "version" : "npm run format && git add -A src",
    "postversion" : "git push && git push --tags"
  },
  "author": "Artur Kornakov",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^3.9.7"
  }
}
